[project]
name = "maps"
version = "0.1.0"
description = "Make and save maps."
dependencies = [
    "flask",
    "psycopg2",
    "bcrypt",
    "jwt",
    "pytz",
    "waitress"
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"
